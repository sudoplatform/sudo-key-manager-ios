platform :ios, "15.0"
use_frameworks!
use_modular_headers!

# Ignore all warnings from pods.
inhibit_all_warnings!

source 'https://cdn.cocoapods.org/'

project 'SudoKeyManager.xcodeproj'

target "SudoKeyManager" do
  podspec :name => 'SudoKeyManager'
end

target "SudoKeyManagerTests" do
  podspec :name => 'SudoKeyManager'
end

target "TestApp" do
  podspec :name => 'SudoKeyManager'
end
